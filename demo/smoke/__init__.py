"""The smallest slice that proves the kit works: DRAFTING <-> GATING.

Spec: demo/SMOKE-SPEC.md. Two model calls, three records, four states - all of
which already exist in slice/records.py, so this runs against the spine exactly
as shipped.
"""
